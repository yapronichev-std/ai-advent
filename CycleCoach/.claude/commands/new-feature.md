Создай новый модуль (или набор файлов) для фичи «{имя_фичи}» в нашем проекте CycleCoach.

Следуй архитектуре, описанной в CLAUDE.md:
- Если фича новая, создай модуль `:feature:{имя_фичи_lowercase}`.
- В модуле создай пакет `{имя_фичи_lowercase}` с подпакетами `presentation`, `domain`, `data` (если нужно).
- В `presentation` создай:
  - `{ИмяФичи}ViewModel` (с `@HiltViewModel`), использующую `StateFlow` и sealed class `{ИмяФичи}UiState`.
  - Compose-экран `{ИмяФичи}Screen`, который наблюдает за состоянием.
  - Навигационный роут (добавь в `app` модуль, если нужно).
- Если требуется работа с данными, создай интерфейс репозитория в `:core:model` и реализацию в `:core:data`.
- Используй Hilt для внедрения зависимостей (конструкторная инъекция).
- Не используй LiveData, XML, Fragment.
- Соблюдай именование согласно кодстайлу из CLAUDE.md.

Все создаваемые файлы должны содержать только необходимый минимум (скелет) — реализации методов пока могут быть заглушками (например, возвращать пустые значения или выкидывать исключения).